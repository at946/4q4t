<div class="container my-2">
  <div class="text-right">
    <i id="how_to_use_icon" class="material-icons modal-open" data-target="how_to_use_modal">help_outline</i>
  </div>
  <h2>
    <%= @team.name %>
    <%= link_to edit_team_path(@team), id: :edit_team_icon do %>
      <i class="material-icons text-accent">edit</i>
    <% end %>
  </h2>
  <p style="overflow: scroll;" class="text-secondary"><%= request.url %></p>
  <p class="mb-2" style="color: #AAA;">URLを共有してメンバーを招待しましょう！</p>
  <%= link_to exercise_path(@team), id: :start_exercise_button, class: "button bg-primary mb-2" do %>
    <span class="text-middle"><i class="material-icons">play_arrow</i>Start exercise</span>
  <% end %>
  <br>

  <%= link_to new_member_path(team: @team.uid), id: 'answer_button', class: 'button mb-1 bg-accent' do %>
    <span class="text-middle"><i class="material-icons">add</i>Member</span>
  <% end %>

  <div class="f-container">
    <% @members.each do |member| %>
      <div class="f-item-12 p-1">
        <div class="card card-primary-left h-100 member-card">
          <div class="mb-1 card-header">
            <h3 class="mr-auto">
              <%= member.name %>
              <span style="font-size: 1rem;">
                <% if member.role.present? %>
                  （<%= member.role %>）
                <% end %>
              </span>
            </h3>
            <%= link_to edit_member_path member do %>
              <i class="material-icons text-accent edit-icon">edit</i>
            <% end %>
          </div>
          <div class="f-container">
            <%= render 'member_card', question: "Q1. #{t :q1}", answers: member.ans_q1 %>
            <%= render 'member_card', question: "Q2. #{t :q2}", answers: member.ans_q2 %>
            <%= render 'member_card', question: "Q3. #{t :q3}", answers: member.ans_q3 %>
            <%= render 'member_card', question: "Q4. #{t :q4}", answers: member.ans_q4 %>    
          </div>
        </div>
      </div>
  <% end %>
  </div>

</div>

<%# modal %>
<div id="modal_overlay"></div>
<div id="how_to_use_modal" class="modal bg-grey">
  <div class="text-right"><i id="close_icon" class="material-icons modal-close">close</i></div>
  <div class="slideshow">
    <%# how to use card %>
    <%= render 'how_to_use_card', title: "1. Answer", image: "how_to_use_1.png", description: "それぞれ４つの質問に回答します。" %>
    <%= render 'how_to_use_card', title: "2. Presentation", image: "how_to_use_2.png", description: "１人ずつ自分の回答を発表します。" %>
    <%= render 'how_to_use_card', title: "3. Feedback", image: "how_to_use_3.png", description: "フィードバックし合います。" %>
    <%# navigation %>
    <i id="slide_prev_icon" class="material-icons slideshow-nav prev">chevron_left</i>
    <i id="slide_next_icon" class="material-icons slideshow-nav next">chevron_right</i>
  </div>
</div>
